# SCAN ROBOT 云端监控部署指南
**Render.com 免费永久部署 · 停电也能监控 · 自动 Telegram 告警**

---

## 第一步：把代码上传到 GitHub

1. 打开 [github.com](https://github.com) → 登录（没有账号就免费注册）
2. 点右上角 **"+"** → **New repository**
3. Repository name 填：`scan-robot-cloud`
4. 选 **Public**（免费必须公开）→ 点 **Create repository**
5. 把 `cloud_server/` 文件夹里的 3 个文件上传上去：
   - `server.js`
   - `package.json`
   - `.gitignore`

   > 方法：点仓库页面的 **"uploading an existing file"** → 拖入文件 → 点 **Commit changes**

---

## 第二步：部署到 Render.com

1. 打开 [render.com](https://render.com) → 点 **Get Started for Free**
2. 用 **GitHub 账号登录**（直接授权，最快）
3. 点 **New** → **Web Service**
4. 选择你刚才创建的 `scan-robot-cloud` 仓库
5. 填写配置：

   | 字段 | 填写内容 |
   |------|----------|
   | Name | `scan-robot-cloud` |
   | Region | `Singapore` (离中国最近，延迟低) |
   | Runtime | `Node` |
   | Build Command | *(留空)* |
   | Start Command | `node server.js` |
   | Instance Type | **Free** |

6. 点 **Advanced** → 添加以下**环境变量**：

   | Key | Value |
   |-----|-------|
   | `TELEGRAM_BOT_TOKEN` | `8867670886:AAG_LayQyLYWV2AI5vd3sv9XSlHh7KY_eNY` |
   | `TELEGRAM_CHAT_ID` | `5911794506` |
   | `OFFLINE_THRESHOLD` | `180` |
   | `ALERT_COOLDOWN` | `600` |

7. 点 **Create Web Service** → 等待约 2 分钟部署完成
8. 部署成功后，Render 会给你一个网址，格式为：
   ```
   https://scan-robot-cloud.onrender.com
   ```

---

## 第三步：设置防休眠（保持 24 小时在线）

Render 免费版 15 分钟无请求会休眠，用 **cron-job.org** 自动 ping 保活：

1. 打开 [cron-job.org](https://cron-job.org) → 免费注册
2. 点 **CREATE CRONJOB**
3. 填写：
   - URL：`https://scan-robot-cloud.onrender.com/ping`
   - 执行频率：选 **Every 10 minutes**（每 10 分钟 ping 一次）
4. 点 **CREATE** → 完成！

---

## 第四步：更新所有设备的配置

把 40 台电脑上的 `config.json` 里的 `dashboard_server_url` 改成你的云端地址：

```json
{
  "dashboard_server_url": "https://scan-robot-cloud.onrender.com"
}
```

> **本机1号机**：直接修改 `C:\Users\work1\Documents\SCAN ROBOT\config.json`
> **其他设备**：重新打包安装包时，在配置里预设好云端地址

---

## 效果对比

| | 原方案（本地服务器）| 新方案（云端服务器）|
|--|--|--|
| 1号机停电 | ❌ 整个监控系统挂掉 | ✅ 云端继续运行 |
| 2~40号机停电 | 🟡 看板显示离线（不告警）| ✅ **3 分钟内推送 Telegram 告警** |
| 全部停电 | ❌ 完全失控 | ✅ **Telegram 持续收到各机器离线告警** |
| 来电恢复 | 需手动启动 | ✅ 设备重新上线后，自动推送恢复通知 |

---

## 告警样例

**停电告警（设备离线时）：**
```
🔴 【设备掉线/停电告警】

📍 设备: DESKTOP-ABC [5号机]
👤 用户: work5
🌐 IP: 192.168.100.205
⏱ 已离线: 4 分钟
🕐 告警时间: 2026-09-24 20:30:00

⚠️ 该设备超过 3 分钟未发送心跳，可能发生了停电、断网或关机！
```

**恢复通知（来电重新上线时）：**
```
🟢 【设备恢复上线通知】

📍 设备: DESKTOP-ABC [5号机]
🕐 恢复时间: 2026-09-24 20:55:00
✅ 设备已重新联网，机器人守护程序恢复运行中！
```
